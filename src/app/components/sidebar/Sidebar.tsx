/**
 * Sidebar legado.
 * A navegação principal do FLOW V3 agora é feita pelo RadialMenu.
 * Mantemos este componente vazio para não quebrar imports antigos durante a migração.
 */
export function Sidebar() {
  return null;
}
